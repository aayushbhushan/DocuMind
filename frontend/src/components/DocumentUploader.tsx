// Handles file selection, drag-and-drop, and upload submission to the API
"use client";

export default function DocumentUploader() {
  return (
    <div className="border-2 border-dashed border-gray-300 rounded-lg p-12 text-center">
      <p className="text-gray-500">Drag and drop a document here, or click to select.</p>
    </div>
  );
}
